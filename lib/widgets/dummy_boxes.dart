import '../export.dart';

class DummyBox15 extends StatelessWidget {
  const DummyBox15({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 15.h,
      width: 15.w,
    );
  }
}
